"use client";

/**
 * SmoothScroll: Bypasses custom wheel interception to allow native, hardware-accelerated
 * browser scroll dynamics. This prevents stuttering and scroll jumping on modern viewports,
 * especially mac trackpads.
 */
export default function SmoothScroll() {
  return null;
}
